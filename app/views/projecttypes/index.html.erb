<%= render 'left' %>
<%= render 'top' %>

<div class="col-lg-12">
  <div class="ibox float-e-margins">
    <div class="ibox-title">
      <h5>项目类型管理</h5>

    </div>
    <div class="ibox-content">

      <table class="table table-hover table-bordered">
        <thead>
        <tr>
          <th>项目类型</th>
          <th>操作</th>
        </tr>
        </thead>
        <tbody>
        <% @projecttypes.each do |projecttype| %>
            <tr>
              <td>
                <%= projecttype.projecttype %>
              </td>
              <td>
                <%= link_to "", edit_projecttype_path(projecttype),class: "fa fa-pencil" %>&nbsp;
                <%= link_to '', projecttype, class: "fa fa-trash", method: :delete, data: { confirm: 'Are you sure?' } %>
              </td>
            </tr>
        <% end %>
        </tbody>
      </table>
      <%= link_to "新增", new_projecttype_path,class: "btn btn-w-m btn-success mylink pull-right" %>
      <br>
    </div>

  </div>
</div>