<%= render 'left' %>
<%= render 'top' %>

<div class="col-lg-12">
  <div class="ibox float-e-margins">
    <div class="ibox-title">
      <h5>语言管理</h5>

    </div>
    <div class="ibox-content">

      <table class="table table-hover table-bordered">
        <thead>
        <tr>
          <th>语言</th>
          <th>操作</th>
        </tr>
        </thead>
        <tbody>
        <% @xmlanguages.each do |language| %>
            <tr>
              <td>
                <%= language.language %>
              </td>
              <td>

                <%= link_to "", edit_xmlanguage_path(language),class: "fa fa-pencil" %>&nbsp;
                <%= link_to '', language, class: "fa fa-trash", method: :delete, data: { confirm: 'Are you sure?' } %>
              </td>
            </tr>
        <% end %>
        </tbody>
      </table>
      <%= link_to "新增", new_xmlanguage_path,class: "btn btn-w-m btn-success mylink pull-right" %>
      <br>
    </div>

  </div>
</div>