<%= render 'left' %>
<%= render 'top' %>

<div class="ibox-content form-horizontal m-t">



  <div class="form-group">
    <label class="col-sm-2 control-label">项目名称</label>

    <div class="col-sm-10">

      <% @yejiatts.each do |f| %>
          <% if f.keyword == 'xmmc' %>
          <div class="m-b">
            <% @languages.each do |flag| %>
                <% if f.langguage_id.to_s == flag.id.to_s %>
                    <%=image_tag(flag.flag.url,width: '35px')  %>
                    <% break %>
                <% end %>
            <% end %>
            <%= f.content %>
          </div>
          <% end %>
      <% end %>
      <%= render 'yejiatts/xmmc' %>
    </div>
  </div>







  <div class="form-group">
    <label class="col-sm-3 control-label">项目名称：</label>
    <div class="col-sm-5">
      sdf<%= @yeji.nameproject %>
    </div>



  </div>

  <div class="form-group">
    <label class="col-sm-3 control-label">工期(年)：</label>
    <div class="col-sm-5">
      <%= @yeji.gongqi %>
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-3 control-label">合同金额(万元)：</label>
    <div class="col-sm-5">
      <%= @yeji.hetonge %>
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-2 control-label">业主名称</label>

    <div class="col-sm-10">

      <% @yejiatts.each do |f| %>
          <% if f.keyword == 'yzmc' %>
              <div class="m-b">
                <% @languages.each do |flag| %>
                    <% if f.langguage_id.to_s == flag.id.to_s %>
                        <%=image_tag(flag.flag.url,width: '35px')  %>
                        <% break %>
                    <% end %>
                <% end %>
                <%= f.content %>
              </div>
          <% end %>
      <% end %>
      <%= render 'yejiatts/yzmc' %>
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-2 control-label">业主地址</label>

    <div class="col-sm-10">

      <% @yejiatts.each do |f| %>
          <% if f.keyword == 'yzdz' %>
              <div class="m-b">
                <% @languages.each do |flag| %>
                    <% if f.langguage_id.to_s == flag.id.to_s %>
                        <%=image_tag(flag.flag.url,width: '35px')  %>
                        <% break %>
                    <% end %>
                <% end %>
                <%= f.content %>
              </div>
          <% end %>
      <% end %>
      <%= render 'yejiatts/yzdz' %>
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-3 control-label">业主电话：</label>
    <div class="col-sm-5">
      <%= @yeji.telyezhu %>
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-3 control-label">付款金额（万元）：</label>
    <div class="col-sm-5">
      <%= @yeji.fukunprice %>
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-3 control-label">合同类型：</label>
    <div class="col-sm-5">
      <%= @hetong.hetong %>
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-3 control-label">国家：</label>
    <div class="col-sm-5">
      <%= @coun.guojia %>
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-3 control-label">实施单位：</label>
    <div class="col-sm-5">
      <%= @yeji.shishidanwei %>
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-3 control-label">项目类型：</label>
    <div class="col-sm-5">
      <%= @projecttype.projecttype %>
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-2 control-label">项目简介</label>

    <div class="col-sm-10">

      <% @yejiatts.each do |f| %>
          <% if f.keyword == 'xmjj' %>
              <div class="m-b">
                <% @languages.each do |flag| %>
                    <% if f.langguage_id.to_s == flag.id.to_s %>
                        <%=image_tag(flag.flag.url,width: '35px')  %>
                        <% break %>
                    <% end %>
                <% end %>
                <%= f.content %>
              </div>
          <% end %>
      <% end %>
      <%= render 'yejiatts/xmjj' %>
    </div>
  </div>



</div>

<%= render 'expands/form' %>