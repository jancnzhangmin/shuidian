<%= render 'left' %>
<%= render 'top' %>

<div class="ibox-content form-horizontal m-t">


  <div class="form-group">
    <label class="col-sm-3 control-label">项目名称</label>
    <div class="col-sm-9">
      <% @yejiatts.each do |f| %>
          <% if f.keyword == 'xmmc' %>
              <div class="m-b">
                <% @languages.each do |flag| %>
                    <% if f.langguage_id.to_s == flag.id.to_s %>
                        <%=image_tag(flag.flag.url,width: '35px')  %>
                        <% break %>
                    <% end %>
                <% end %>
                <%= f.content %>
                &nbsp;<a href=<%=edit_yeji_yejiatt_path(@yeji,f)  %> data-toggle="modal" data-target="#myModal"><i class="fa fa-pencil"></i></a>
                <%= link_to '', [@yeji,f], class: "fa fa-trash", method: :delete, data: { confirm: 'Are you sure?' } %>
              </div>
          <% end %>
      <% end %>
      <%= render 'yejiatts/xmmc' %>
    </div>
  </div>

  <!-- 模态框（Modal） -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog"
       aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close"
                  data-dismiss="modal" aria-hidden="true">
            &times;
          </button>
          <h4 class="modal-title" id="myModalLabel">
            模态框（Modal）标题
          </h4>
        </div>
        <div class="modal-body">
          在这里添加一些文本
        </div>
        <div class="modal-footer">
          <button type="button" class="btn btn-default"
                  data-dismiss="modal">关闭
          </button>
          <button type="button" class="btn btn-primary">
            提交更改
          </button>
        </div>
      </div><!-- /.modal-content -->
    </div><!-- /.modal -->
  </div>





<div class="form-group">
  <label class="col-sm-3 control-label">业主名称</label>

  <div class="col-sm-9">

    <% @yejiatts.each do |f| %>
        <% if f.keyword == 'yzmc' %>
            <div class="m-b">
              <% @languages.each do |flag| %>
                  <% if f.langguage_id.to_s == flag.id.to_s %>
                      <%=image_tag(flag.flag.url,width: '35px')  %>
                      <% break %>
                  <% end %>
              <% end %>
              <%= f.content %>
              &nbsp;<a href=<%=edit_yeji_yejiatt_path(@yeji,f)  %> data-toggle="modal" data-target="#myModal"><i class="fa fa-pencil"></i></a>
              <%= link_to '', [@yeji,f], class: "fa fa-trash", method: :delete, data: { confirm: 'Are you sure?' } %>
            </div>
        <% end %>
    <% end %>
    <%= render 'yejiatts/yzmc' %>
  </div>
</div>

  <div class="form-group">
    <label class="col-sm-3 control-label">业主地址</label>

    <div class="col-sm-9">

      <% @yejiatts.each do |f| %>
          <% if f.keyword == 'yzdz' %>
              <div class="m-b">
                <% @languages.each do |flag| %>
                    <% if f.langguage_id.to_s == flag.id.to_s %>
                        <%=image_tag(flag.flag.url,width: '35px')  %>
                        <% break %>
                    <% end %>
                <% end %>
                <%= f.content %>
                &nbsp;<a href=<%=edit_yeji_yejiatt_path(@yeji,f)  %> data-toggle="modal" data-target="#myModal"><i class="fa fa-pencil"></i></a>
                <%= link_to '', [@yeji,f], class: "fa fa-trash", method: :delete, data: { confirm: 'Are you sure?' } %>
              </div>
          <% end %>
      <% end %>
      <%= render 'yejiatts/yzdz' %>
    </div>
  </div>

  <div class="form-group">
    <label class="col-sm-3 control-label">项目简介</label>

    <div class="col-sm-9">

      <% @yejiatts.each do |f| %>
          <% if f.keyword == 'xmjj' %>
              <div class="m-b">
                <% @languages.each do |flag| %>
                    <% if f.langguage_id.to_s == flag.id.to_s %>
                        <%=image_tag(flag.flag.url,width: '35px')  %>
                        <% break %>
                    <% end %>
                <% end %>
                <%= f.content %>
                &nbsp;<a href=<%=edit_yeji_yejiatt_path(@yeji,f)  %> data-toggle="modal" data-target="#myModal"><i class="fa fa-pencil"></i></a>
                <%= link_to '', [@yeji,f], class: "fa fa-trash", method: :delete, data: { confirm: 'Are you sure?' } %>
              </div>
          <% end %>
      <% end %>
      <%= render 'yejiatts/xmjj' %>
    </div>
  </div>



  <%= render 'form' %>


  <%= render 'expands/form' %>







</div>