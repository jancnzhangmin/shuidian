<%= render 'left' %>
<%= render 'top' %>

<div class="col-lg-12">
  <div class="ibox float-e-margins">
    <div class="ibox-title">
      <h5>合同类型管理</h5>

    </div>
    <div class="ibox-content">

      <table class="table table-hover table-bordered">
        <thead>
        <tr>
          <th>合同类型</th>
          <th>操作</th>
        </tr>
        </thead>
        <tbody>
        <% @hetongs.each do |hetong| %>
            <tr>
              <td>
                <%= hetong.hetong %>
              </td>
              <td>
                <%= link_to "", edit_hetong_path(hetong),class: "fa fa-pencil" %>&nbsp;
                <%= link_to '', hetong, class: "fa fa-trash", method: :delete, data: { confirm: 'Are you sure?' } %>
              </td>
            </tr>
        <% end %>
        </tbody>
      </table>
      <%= link_to "新增", new_hetong_path,class: "btn btn-w-m btn-success mylink pull-right" %>
      <br>
    </div>

  </div>
</div>