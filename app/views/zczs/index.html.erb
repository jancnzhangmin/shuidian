<%= render 'left' %>
<%= render 'top' %>

<div class="col-lg-12">
  <div class="ibox float-e-margins">
    <div class="ibox-title">
      <h5>职称证专业管理</h5>

    </div>
    <div class="ibox-content">

      <table class="table table-hover table-bordered">
        <thead>
        <tr>
          <th>职称证专业</th>
          <th>操作</th>
        </tr>
        </thead>
        <tbody>
        <% @zczs.each do |zcz| %>
            <tr>
              <td>
                <%= zcz.zcz %>
              </td>
              <td>
                <%= link_to "", edit_zcz_path(zcz),class: "fa fa-pencil" %>&nbsp;
                <%= link_to '', zcz, class: "fa fa-trash", method: :delete, data: { confirm: 'Are you sure?' } %>
              </td>
            </tr>
        <% end %>
        </tbody>
      </table>
      <%= link_to "新增", new_zcz_path,class: "btn btn-w-m btn-success mylink pull-right" %>
      <br>
    </div>

  </div>
</div>